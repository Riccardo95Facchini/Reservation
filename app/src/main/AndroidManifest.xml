<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="facchini.riccardo.reservation">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity_Customer_SelectedShop"></activity>

        <!-- Login activity -->
        <activity android:name=".Activity_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Create customer activity -->
        <activity
            android:name=".Activity_Customer_Create"
            android:label="@string/customerRegistration"
            android:parentActivityName=".Activity_Login" />

        <!-- Create customer activity -->
        <activity
            android:name=".Activity_Shop_Create"
            android:label="@string/shopRegistration"
            android:parentActivityName=".Activity_Login" />

        <!-- Add tags and hours activity -->
        <activity
            android:name=".Activity_Shop_TagHours"
            android:label="@string/tagsAndHours" />

        <!-- Customer Activity -->
        <activity android:name=".Activity_Customer" />

        <!-- Shop Activity -->
        <activity android:name=".Activity_Shop" />


    </application>

</manifest>